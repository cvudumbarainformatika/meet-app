import { boot } from 'quasar/wrappers'

// Boot file untuk Tailwind CSS
// CSS sudah di-import di quasar.config.js, jadi boot file ini hanya placeholder
export default boot(() => {
  // Tailwind CSS sudah di-load via quasar.config.js css array
})